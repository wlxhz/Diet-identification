# 安全说明

## 当前定位

本项目当前处于原型和本地联调阶段。默认启动方式不是生产部署方案，不应直接暴露到公网。

## 敏感数据

以下内容不得提交到版本库：

- Flask 会话密钥和管理员密钥。
- 用户数据库、管理员数据库及其备份。
- 用户头像、用餐关键帧和完整视频。
- 手机号、邮箱、健康指标和绑定关系导出。
- 真实设备日志中可能包含的标识符和网络信息。
- Android 正式签名密钥。

本机敏感数据统一放在 `.workspace/data/`，配置参考 `.env.example`。

## 生产部署最低要求

- 使用 HTTPS 和可信反向代理。
- 使用生产级 WSGI/ASGI 服务，不使用 Flask 开发服务器。
- 密钥由密钥管理系统或环境变量注入。
- 数据库采用访问控制、备份、加密和最小权限。
- WebSocket/SSE 连接必须鉴权并在每次订阅时校验共享关系。
- 管理端和业务监管者权限必须分离。
- 所有记录修改、删除、确认和权限变化必须审计。
- 对登录、验证码、识别上传和实时连接实施速率限制。
- 上传图片必须限制类型、尺寸和解码资源消耗。

## 漏洞报告

不要在公开 Issue 中提交真实用户数据、密钥或可利用细节。请通过项目负责人指定的私有渠道报告，并包含影响范围、复现步骤和建议缓解措施。
